EXPO_PUBLIC_SERVER_URL=http://YOUR_LOCAL_IP:3001
# Use your machine's local network IP (not localhost) so the phone can reach it.
# Find it with: ifconfig | grep "inet " (Mac/Linux) or ipconfig (Windows)
# Example: EXPO_PUBLIC_SERVER_URL=http://192.168.1.42:3001
